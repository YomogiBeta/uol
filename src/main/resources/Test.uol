class Test extends Object {

    public function test(){
        anIO = new IO()
        if (1 == 1){
            if (2 == 2){
                anIO.println("oop!")
                // return nil
            } else {
                anIO.println("nooop!")
                // return nil
            }
        }
        aNumber = you.calculate() // youというシュード変数を利用できる
        anIO.println(aNumber) // 標準出力へ数値の出力ができる

    }
}
//if文、if-else文が書ける
