import { Test } from "Test" // 他のソースからのインポートができる

class Main extends Object {

    private aNumber = 2

    public function getOtherNumber() {
        return self.aNumber // チェインが利用できる
    }

    public function calculate() {
        aTest = 255
        return (aTest + 1) * (self.getOtherNumber())  // 計算式を記述できる・selfというシュード変数を利用できる

    }

    public function main(){
        anIO = new IO()
        aTest = new Test()
        aTest.test()
    }
}

// メンバとメソッドを持つ基本的なクラス定義ができる
// クラス継承ができる
